# domain

Engines de domínio puros (sem I/O, sem Prisma, sem HTTP): `RatingCalculator`, `EloEngine`, `AchievementEngine`, `RivalryEngine`. Recebem dados em memória, devolvem resultados. Testáveis isoladamente e reutilizados tanto pela API quanto pelo importador.
